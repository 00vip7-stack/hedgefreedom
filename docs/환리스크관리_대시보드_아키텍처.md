# 환리스크 관리 대시보드 아키텍처

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [기술 스택](#기술-스택)
3. [시스템 구조](#시스템-구조)
4. [데이터 흐름](#데이터-흐름)
5. [핵심 모듈](#핵심-모듈)
6. [보안 구조](#보안-구조)
7. [배포 전략](#배포-전략)

---

## 🎯 시스템 개요

### 목적
중소기업의 환리스크를 **실시간으로 측정, 분석, 헤지**하는 통합 관리 시스템

### 핵심 기능
- ✅ **26개 전문 계산기** (VaR, Monte Carlo, 최적 헤지 비율 등)
- ✅ **실시간 환율 모니터링** (1분 단위 수집)
- ✅ **자동 헤지 전략 추천**
- ✅ **효과성 평가** (월별 자동 리포트)
- ✅ **감사 추적** (모든 거래/계산 이력 보존)

---

## 🛠 기술 스택

### Backend
```javascript
Node.js 18.x
Express.js (웹 서버)
express-session (세션 관리)
bcryptjs (암호화)
helmet (보안 헤더)
node-cron (정기 작업)
```

### Frontend
```javascript
Vanilla JavaScript (Zero 프레임워크)
Chart.js (실시간 차트)
HTML5 + CSS3
```

### 데이터
```
JSON 기반 파일 시스템 (No DB)
├─ input/       (고객 거래 데이터)
├─ source/      (외부 API 데이터)
└─ computed/    (계산 결과)
```

### 외부 API
```
frankfurter.app     (실시간 환율)
FRED API            (미국 경제 지표)
한국은행 ECOS       (한국 경제 지표)
수출입은행          (공식 고시환율)
CBOE                (VIX 지수)
```

---

## 🏗 시스템 구조

```
┌─────────────────────────────────────────────────────┐
│                   사용자 브라우저                      │
│  (Chrome/Edge/Safari - 로그인, 거래입력, 대시보드)    │
└────────────────┬────────────────────────────────────┘
                 │ HTTPS
                 ▼
┌─────────────────────────────────────────────────────┐
│                Express.js 서버                        │
│  ┌─────────────────────────────────────────────┐   │
│  │ Middleware Layer                             │   │
│  │  • helmet (보안)                             │   │
│  │  • express-session (세션)                    │   │
│  │  • cookieParser                              │   │
│  │  • rateLimit (API 제한)                      │   │
│  │  • securityValidator (입력 검증)             │   │
│  └─────────────────────────────────────────────┘   │
│                                                      │
│  ┌─────────────────────────────────────────────┐   │
│  │ API Routes                                   │   │
│  │  • /api/trades       (거래 관리)            │   │
│  │  • /api/calculator   (계산기 실행)          │   │
│  │  • /api/dashboard    (대시보드 데이터)      │   │
│  │  • /api/contract     (헤지 계약)            │   │
│  │  • /api/effectiveness (효과성 평가)         │   │
│  └─────────────────────────────────────────────┘   │
│                                                      │
│  ┌─────────────────────────────────────────────┐   │
│  │ Background Engine (백그라운드 계산 엔진)     │   │
│  │  • 실시간 데이터 수집 (1분마다)             │   │
│  │  • 거래별 자동 계산 (26개 계산기)           │   │
│  │  • 정기 작업 스케줄링 (cron)                │   │
│  │  • 효과성 평가 (매월 1일)                   │   │
│  └─────────────────────────────────────────────┘   │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│              계산 엔진 (lib/)                         │
│  ┌─────────────────────────────────────────────┐   │
│  │ 26개 전문 계산기                             │   │
│  │  01. 최적 헤지 비율 (MVHR)                  │   │
│  │  02. 헤지 도구 추천                         │   │
│  │  03. 결제일 예상환율                        │   │
│  │  04. 예상 손익                              │   │
│  │  07. VaR (Value at Risk)                    │   │
│  │  10. Monte Carlo 시뮬레이션                 │   │
│  │  15. VIX 시장 충격 지수                     │   │
│  │  ... 나머지 19개                            │   │
│  └─────────────────────────────────────────────┘   │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│              데이터 계층 (data/)                      │
│                                                      │
│  input/ (고객 입력)                                  │
│  ├─ trades/CUST-001/                                │
│  │   └─ TRADE-001.json                             │
│  └─ profiles/CUST-001.json                          │
│                                                      │
│  source/ (외부 API)                                 │
│  ├─ fx-rates/minute/USDKRW_20260127_0030.json      │
│  ├─ vix/hourly/VIX_2026012700.json                 │
│  └─ macro/monthly/FRED_2026-01.json                │
│                                                      │
│  computed/ (계산 결과 - Immutable)                  │
│  └─ trades/CUST-001/TRADE-001/                     │
│      ├─ 20260127_143022.json (타임스탬프별 보존)   │
│      └─ latest.json                                 │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 데이터 흐름

### 1️⃣ 거래 입력 플로우

```
사용자
  │
  │ 1. 거래 입력
  ▼
POST /api/trades
  │
  │ 2. 입력 검증
  ▼
securityValidator
  │
  │ 3. 거래 저장
  ▼
data/input/trades/CUST-001/TRADE-001.json
  │
  │ 4. 자동 계산 트리거
  ▼
BackgroundEngine.calculateForTrade()
  │
  ├─ 5a. 외부 데이터 수집
  │   ├─ frankfurter.app (환율)
  │   ├─ FRED (거시경제)
  │   └─ ECOS (한국 데이터)
  │
  ├─ 5b. 26개 계산기 실행
  │   ├─ calculator-01 (최적 헤지 비율)
  │   ├─ calculator-03 (예상환율)
  │   ├─ calculator-04 (손익)
  │   └─ ... (나머지 23개)
  │
  └─ 6. 결과 저장 (타임스탬프)
      ▼
  data/computed/trades/CUST-001/TRADE-001/20260127_143022.json
  │
  │ 7. 감사 로그
  ▼
audit-logs/2026-01/27.log
```

### 2️⃣ 실시간 데이터 수집 (자동)

```
node-cron (1분마다)
  │
  ▼
BackgroundEngine.collectLiveData()
  │
  ├─ frankfurter.app API
  │   ▼
  │  data/source/fx-rates/minute/USDKRW_20260127_0835.json
  │
  ├─ VIX API (시간별)
  │   ▼
  │  data/source/vix/hourly/VIX_2026012708.json
  │
  └─ FRED/ECOS (24시간마다)
      ▼
     data/source/macro/monthly/
```

### 3️⃣ 대시보드 조회

```
GET /api/dashboard
  │
  ├─ 1. 최신 계산 결과 로드
  │     data/computed/trades/*/latest.json
  │
  ├─ 2. 실시간 환율 로드
  │     data/source/fx-rates/live.json
  │
  ├─ 3. VIX 지수
  │     data/source/vix/live.json
  │
  └─ 4. 통합 대시보드 데이터 생성
      ▼
     JSON Response → 브라우저 렌더링
```

---

## 🔧 핵심 모듈

### 1. server.js (메인 서버)
```javascript
// 역할: HTTP 서버, 라우팅, 미들웨어
const PORT = 7860; // 허깅페이스 호환

// 보안 미들웨어
app.use(helmet());
app.use(session({ secret: SESSION_SECRET }));

// 핵심 라우트
app.post('/api/trades', tradeController);
app.get('/api/calculator/:id', calculatorController);
app.get('/api/dashboard', dashboardController);
```

### 2. background-engine.js (백그라운드 엔진)
```javascript
// 역할: 자동 계산, 데이터 수집, 정기 작업

class BackgroundEngine {
  // 거래별 26개 계산기 실행
  async calculateForTrade(tradeId) {
    const results = {};
    results.hedgeRatio = await calculator01.calculate();
    results.expectedRate = await calculator03.calculate();
    // ... 나머지 24개
    
    // 타임스탬프별 저장 (Immutable)
    this.saveResult(tradeId, results, timestamp);
  }
  
  // 실시간 데이터 수집 (1분마다)
  async collectLiveData() {
    await this.fetchFXRates();
    await this.fetchVIX();
  }
}
```

### 3. calculator-01-hedge-ratio.js (최적 헤지 비율)
```javascript
// MVHR 알고리즘
function calculateMVHR(trade, marketData) {
  const rho = marketData.correlation; // 상관계수
  const sigmaSpot = marketData.spotVolatility;
  const sigmaForward = marketData.forwardVolatility;
  
  const hedgeRatio = rho * (sigmaSpot / sigmaForward);
  
  return {
    ratio: hedgeRatio,
    hedgeAmountKRW: trade.amount_krw * hedgeRatio
  };
}
```

### 4. audit-logger.js (감사 추적)
```javascript
// 모든 거래/계산 이력 보존
function logDataChange(action, userId, data) {
  const logEntry = {
    timestamp: new Date().toISOString(),
    action,
    userId,
    data,
    ip: req.ip
  };
  
  fs.appendFileSync(
    `audit-logs/${YYYY-MM}/${DD}.log`,
    JSON.stringify(logEntry) + '\n'
  );
}
```

---

## 🔒 보안 구조

### 1. 3계층 데이터 분리
```
로컬 개발 환경
├─ 실제 고객 데이터 (CUST-001, CUST-002...)
├─ 테스트 데이터 (ADMIN-*, TEST-*, DEMO-*)
└─ API 키 (.env)

GitHub (Private Repository)
├─ 소스 코드만
├─ 테스트 데이터
└─ 고객 데이터 제외 (.gitignore)

Hugging Face Space (Public)
├─ 실행 가능한 코드
├─ 빈 데이터 구조만 (.gitkeep)
└─ 민감 데이터 완전 차단
```

### 2. .gitignore (민감 데이터 차단)
```gitignore
# 환경 변수
.env
.env.local

# 고객 데이터
backend/data/input/trades/*/
backend/data/input/profiles/*/
!backend/data/input/trades/ADMIN-*
!backend/data/input/trades/TEST-*
!backend/data/input/trades/DEMO-*

# 계산 결과
backend/data/computed/trades/*/

# 세션
backend/sessions/*

# 감사 로그
audit-logs/
```

### 3. 보안 미들웨어
```javascript
// Helmet (보안 헤더)
app.use(helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'", "'unsafe-inline'"],
      styleSrc: ["'self'", "'unsafe-inline'"]
    }
  }
}));

// Rate Limiting (API 남용 방지)
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15분
  max: 100 // 최대 100회
});

// 입력 검증
function validateTradeInput(data) {
  if (!data.amount_ccy || data.amount_ccy < 0) {
    throw new Error('Invalid amount');
  }
  // XSS 방지
  data.memo = sanitizeHtml(data.memo);
}
```

---

## 🚀 배포 전략

### 로컬 개발 환경
```bash
# .env 설정
DEV_MODE=true
SESSION_SECRET=dev-secret
PORT=3000

# 서버 시작
cd d:\hedgeBasic\backend
node server.js

# 접속
http://localhost:3000
```

### Hugging Face Space (Docker)
```dockerfile
FROM node:18-alpine

WORKDIR /app

# 의존성 설치
COPY backend/package*.json ./
RUN npm ci --only=production

# 필수 파일만 복사
COPY backend/server.js ./
COPY backend/lib/ ./lib/
COPY backend/public/ ./public/

# 환경 변수
ENV PORT=7860
ENV NODE_ENV=production

# 서버 시작
CMD ["node", "server.js"]
```

### GitHub Actions (자동 배포)
```yaml
name: Deploy to Hugging Face

on:
  push:
    branches: [main]

jobs:
  security-check:
    runs-on: ubuntu-latest
    steps:
      - name: 민감 데이터 검사
        run: |
          # .env 파일 존재 확인
          if [ -f backend/.env ]; then
            echo "❌ .env 파일 발견!"
            exit 1
          fi
          
          # 실제 고객 데이터 확인
          REAL_FILES=$(find backend/data/input/trades -name "*.json" \
            ! -path "*/ADMIN-*" \
            ! -path "*/TEST-*" \
            ! -path "*/DEMO-*" | wc -l)
          
          if [ "$REAL_FILES" -gt 0 ]; then
            echo "❌ 실제 고객 데이터 발견!"
            exit 1
          fi

  deploy:
    needs: security-check
    runs-on: ubuntu-latest
    steps:
      - name: Hugging Face 배포
        run: |
          git push https://$HF_TOKEN@huggingface.co/spaces/00vip/hedgebasic
```

---

## 📊 성능 최적화

### 1. 데이터 캐싱
```javascript
// 실시간 환율 (1분 캐시)
let cachedFXRate = null;
let cacheTime = null;

async function getFXRate() {
  if (cacheTime && Date.now() - cacheTime < 60000) {
    return cachedFXRate;
  }
  
  cachedFXRate = await fetchFromAPI();
  cacheTime = Date.now();
  return cachedFXRate;
}
```

### 2. 계산 결과 재사용
```javascript
// latest.json 파일 활용
function getLatestCalculation(tradeId) {
  return readJson(`computed/trades/${tradeId}/latest.json`);
}
```

### 3. 비동기 처리
```javascript
// 26개 계산기 병렬 실행
const results = await Promise.all([
  calculator01.calculate(trade),
  calculator02.calculate(trade),
  // ... 나머지 24개
]);
```

---

## 🔄 확장성

### 향후 추가 가능 기능
- ✅ PostgreSQL/MongoDB 연동 (JSON → DB)
- ✅ Redis 캐싱
- ✅ Webhook 알림 (Slack, 이메일)
- ✅ REST API → GraphQL
- ✅ 모바일 앱 (React Native)
- ✅ AI/ML 환율 예측 모델

---

## 📝 개발 원칙

### Zero Hardcoding
```javascript
// ❌ 나쁜 예
const hedgeRatio = 0.85;

// ✅ 좋은 예
const hedgeRatio = calculateMVHR(trade, marketData);
```

### Immutable Data
```javascript
// 계산 결과는 절대 수정 안 함
const resultFile = `${tradeId}_${timestamp}.json`;
fs.writeFileSync(resultFile, JSON.stringify(result));
// 이후 이 파일은 읽기 전용
```

### Append-Only Logs
```javascript
// 모든 이력 보존 (삭제 금지)
audit-logs/
  └─ 2026-01/
      ├─ 01.log
      ├─ 02.log
      └─ 27.log  ← 오늘 로그 (계속 추가만)
```

---

**작성일**: 2026-01-27  
**버전**: 1.0  
**작성자**: GitHub Copilot
